-- nama databasenya LabAssetManagement
-- This script was generated by a beta version of the ERD tool in pgAdmin 4.
-- Please log an issue at https://redmine.postgresql.org/projects/pgadmin4/issues/new if you find any bugs, including reproduction steps.
-- contoh editing 1 file
BEGIN;


CREATE TABLE IF NOT EXISTS public.assetcategory
(
    category_id integer NOT NULL GENERATED ALWAYS AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    asset_name character varying(255) COLLATE pg_catalog."default" NOT NULL,
    asset_qty integer,
    asset_kode_prodi prodi NOT NULL,
    num_approver integer DEFAULT 1,
    CONSTRAINT assetcategory_pkey PRIMARY KEY (category_id)
);

CREATE TABLE IF NOT EXISTS public.assets
(
    asset_id integer NOT NULL DEFAULT nextval('assets_asset_id_seq'::regclass),
    asset_sn character varying(20) COLLATE pg_catalog."default",
    asset_status character varying(255) COLLATE pg_catalog."default" NOT NULL,
    asset_assigned_location text COLLATE pg_catalog."default",
    asset_curr_location text COLLATE pg_catalog."default",
    asset_brand character varying(255) COLLATE pg_catalog."default",
    asset_booked_date json,
    category_id integer NOT NULL,
    CONSTRAINT assets_pkey PRIMARY KEY (asset_id)
);

CREATE TABLE IF NOT EXISTS public.prodiv
(
    kode_prodiv prodi NOT NULL,
    num_approver integer DEFAULT 1
);

CREATE TABLE IF NOT EXISTS public.requests
(
    request_id integer NOT NULL DEFAULT nextval('requests_request_id_seq'::regclass),
    request_date timestamp without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
    book_date timestamp without time zone NOT NULL,
    return_date timestamp without time zone NOT NULL,
    request_reason text COLLATE pg_catalog."default" NOT NULL,
    request_status character varying(255) COLLATE pg_catalog."default",
    request_items json NOT NULL,
    user_id integer NOT NULL,
    return_condition text COLLATE pg_catalog."default",
    track_approver integer DEFAULT 0,
    num_approver integer DEFAULT 1,
    taken_date timestamp without time zone,
    realize_return_date timestamp without time zone,
    flag_return boolean,
    CONSTRAINT requests_pkey PRIMARY KEY (request_id)
);

CREATE TABLE IF NOT EXISTS public.users
(
    user_id integer NOT NULL GENERATED ALWAYS AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 2147483647 CACHE 1 ),
    username character varying(255) COLLATE pg_catalog."default",
    binusian_id character(11) COLLATE pg_catalog."default",
    user_email character varying(255) COLLATE pg_catalog."default",
    user_password character varying(255) COLLATE pg_catalog."default" NOT NULL,
    user_phone character varying(12) COLLATE pg_catalog."default",
    user_address text COLLATE pg_catalog."default",
    user_kode_prodiv prodi,
    user_role roles NOT NULL,
    CONSTRAINT users_pkey PRIMARY KEY (user_id)
);

ALTER TABLE IF EXISTS public.assets
    ADD CONSTRAINT fk_assetcategory FOREIGN KEY (category_id)
    REFERENCES public.assetcategory (category_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.requests
    ADD CONSTRAINT fk_requestpic FOREIGN KEY (user_id)
    REFERENCES public.users (user_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;

END;